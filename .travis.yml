sudo: required

language: generic

services:
  - docker

before_install:
  - echo "start creating an image with dockerfile"
  - docker build -t nnyong0511/tavis-docker-react-app -f Dokerfile.dev .

  script:
  - docker run -e CI=true nnyong0511/tavis-docker-react-app npm run test -- --coverage

after_success:
  - echo "Test success"